{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "C++ Build",
      "type": "shell",
      "command": "clang++",
      "args": ["-g", "-std=c++98", "main.cpp", "-o", "main"],
      "group": "build"
    },
    {
      "label": "C++ Watch and Run",
      "type": "shell",
      "command": "bash -c 'compile_and_run() { clear; pkill -f \"./main.o\" 2>/dev/null; g++ -std=c++98 -pedantic -o main.o \"$1\" && ./main.o & }; compile_and_run \"$1\"; fswatch -o \"$1\" | while read; do compile_and_run \"$1\"; done' _ ${file}",
      "group": {
        "isDefault": true,
        "kind": "build"
      }
    }
  ]
}
